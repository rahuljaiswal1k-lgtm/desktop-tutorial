# Drop images here

Place your **profile & general images (e.g. rahul-jaiswal.jpg)** here, then reference them in `assets/data/portfolio.json`
using the path shown next to each entry. Recommended formats: `.jpg` / `.webp`
(≈1600px wide, optimised). Until a real image exists, the site shows a
premium gradient placeholder automatically — nothing breaks.
